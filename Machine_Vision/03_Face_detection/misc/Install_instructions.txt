# See install intructions for face-recognition:

-- Don't install ffmpeg from aptitude if user wants face recognition installed --
sudo apt-get install -y ffmpeg libav-tools

sudo pip3 install sk-video